# Exemplo de ficheiro mysqldump --defaults-extra-file.
# Em produção pode reutilizar o ficheiro legado em BaZe_Management/config/baze2_db.conf
# ou copiar credenciais seguras para secrets/mariadb-dump.cnf (não versionar valores reais).
#
# [client]
# user=backup_user
# password=change-me
# host=127.0.0.1
