# Exemplo de ficheiro mysqldump --defaults-extra-file.
# Copiar para secrets/mariadb-dump.cnf com credenciais reais (não versionar).
#
# [client]
# user=backup_user
# password=change-me
# host=127.0.0.1
