# Copiar para .env.prod-sync (opcional; gitignore se contiver dados sensíveis).
# Host/user/port/runtime root vêm normalmente de secrets/production.deploy.local.env

WARDEN_SYNC_MODE=scp

# Sobrescrever apenas se necessário:
# WARDEN_RUNTIME_ROOT=/opt/warden
# WARDEN_DEPLOY_SSH_PORT=22
